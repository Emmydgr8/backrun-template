[project]
name = "backrun-template"
authors = ["Backrun Developer"]
description = "A decentralized asset registry and marketplace protocol"
telemetry = false
requirements = []
[contracts.backrun-registry]
path = "contracts/backrun-registry.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
